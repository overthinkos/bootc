candy:
    version: 2026.144.1443
    name: bootc-base
    # sshd + qemu-guest-agent stay in main (shared across repos) → pulled by
    # @github ref. bootc-config is bootc-exclusive and vendored locally beside
    # this layer (resolved via this repo's discover: block).
    candy:
        - '@github.com/overthinkos/overthink/candy/sshd:v2026.159.1458'
        - '@github.com/overthinkos/overthink/candy/qemu-guest-agent:v2026.159.1458'
        - bootc-config
